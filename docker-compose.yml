version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    container_name: mgv-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: k6k2tHwYChVlRTQg
      POSTGRES_DB: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

volumes:
  postgres_data:
