import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { await prisma.featureFlag.upsert({ where: { key: "STOCK_RESERVATION" }, update: { value: true }, create: { key: "STOCK_RESERVATION", value: true, description: "Reserva de estoque" } }); await prisma.featureFlag.upsert({ where: { key: "OS_PROFITABILITY_CALC" }, update: { value: true }, create: { key: "OS_PROFITABILITY_CALC", value: true, description: "Apuração de rentabilidade" } }); console.log("Habilitado!"); } main().finally(() => prisma.$disconnect());
